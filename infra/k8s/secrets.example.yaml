# Copie para secrets.yaml e preencha com valores reais (não commitar secrets.yaml)
# cp secrets.example.yaml secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: lphajir-secrets
  namespace: lphajir
type: Opaque
stringData:
  APP_SECRET: "COLOQUE_SEU_SEGREDO_FORTE_AQUI"
  # Credenciais do CRM PHD (dados sensíveis)
  CRM_ADMIN_EMAIL: "admin@hajir"
  CRM_ADMIN_PASSWORD: "admin123"
  RESEND_API_KEY: "re_xxxxxxxx"
